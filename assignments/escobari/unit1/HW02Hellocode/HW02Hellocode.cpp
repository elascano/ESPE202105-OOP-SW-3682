#include<stdio.h>
#include<stdlib.h>
#include<conio.h> 

using namespace std;

int main(){
	int array_4=(12,23,34,98,87,65,0);
	int sum()
	int average()
	int resolution()
	return 0;
}
int sum(){
	for (int i=0; i<4;i++){
		do{
			cout<<"Ingrese los numeros: "<<i+1<<endl;
			cin>>nota[i];
			
		}while(nota[i]<0 || nota[i]>20);
		
		sum=sum+sum[i];
	}	
}
int average(){
	float array average[averag]
	float average;
	printf(insert the number)
	for(int i=0; i<averag;i++)
}
int resolution()
int a=0
printf("number to the resolution");
sccanf("%d,&number");
if (a==1)
{
	printf("number to exist");
}
else
{
	printf("number don't exist'")
}

return 0;

